query GetCountriesByContinent($continentCode: String!) {
  countriesByContinent(continentCode: $continentCode) {
    code
    name
    capital
    currency
    emoji
    continent {
      code
      name
    }
  }
}
